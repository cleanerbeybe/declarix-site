# Follow-on queue

## After deployment and manual QA

1. Confirm the canonical report, CSV, and SVG return `200` on the live GitHub Pages domain.
2. Validate the `Report`, `Dataset`, and breadcrumb graph in a structured-data parser.
3. Confirm sitemap and both LLM discovery files contain the canonical route.
4. Check live download and booking events in the configured analytics destination.
5. Publish the first distribution wave from `CONTENT-DISTRIBUTION.md`.

## Held until the build stream completes

- Google Search Console submission and query monitoring;
- IndexNow submission;
- newsletter provider selection and public signup implementation;
- any row-level company directory, lead database, or gated prospect export.

## Potential report expansions

- annual update using the same aggregate contract;
- a separate customs-operations capacity benchmark using anonymous, permissioned buyer inputs rather than prospect research;
- region analysis only after normalization and a documented minimum-cell disclosure rule;
- an embed-ready PNG chart set if editorial outreach shows demand beyond the vector source;
- a dedicated research index after at least three durable reports exist.
